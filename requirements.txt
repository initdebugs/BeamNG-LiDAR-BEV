# 1.36 speaks bridge protocol v1.27 = BeamNG.tech 0.39.x. It CANNOT talk to
# 0.38.5 (v1.26) -- the hello() handshake refuses -- so this pin and
# config.BEAMNG_EXE move together. See CLAUDE.md "Commands".
beamngpy==1.36
numpy>=1.26,<2.0
# <6.8: PyQt6 6.11 could not be imported on this machine's Python 3.11.0
# (DLL procedure error); 6.7.x loads fine. See CLAUDE.md "Commands".
PyQt6>=6.6,<6.8
# capture.py encodes recorded camera frames as JPEG on its writer thread.
# Imported lazily there, so a missing Pillow costs recording rather than the app.
Pillow>=10.0
