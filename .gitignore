/.claude
# Agent implementation workspaces; both are local, disposable coordination
# state and must never be committed with product changes.
/.worktrees/
/.superpowers/sdd/

# Runtime logs; the colour-probe dump beside them stays tracked as evidence.
logs/*.log
logs/*.log.*

# Bytecode caches; regenerated by the interpreter.
__pycache__/
*.pyc

# The per-simulator virtualenv (see install_dependencies.bat).
.venv39/

# Throwaway camera captures from tools/capture_cameras.py (96 MB of them
# during one investigation). The two frames that are actual evidence live
# beside the tool as tools/vision_alpha_{bug,fixed}.png.
tools/camera_probe/

# Stereo captures from tools/kerb_experiment.py: 6-13 MB per pair, and
# reproducible by re-running the capture. The verdict they produced lives
# in docs/VISION_ROADMAP.md phase 1.
tools/kerb_data/
# The oracle's lockstep captures (20+ MB each); the side-by-side PNG beside
# them stays tracked as evidence, like the colour probe.
tools/oracle_data/*.npz

# tools/hybrid_rig_probe.py's side-by-side output; re-run the probe for it.
tools/hybrid_rig_probe.png

# Training captures: hundreds of MB of camera frames per drive, and
# reproducible by recording again. The labels inside them are NOT reproducible,
# so copy a session out of here before deleting one.
captures/

# Overlay renders from tools/label_overlay.py; re-run the tool for them.
tools/label_overlay/

# Training masks built from captures/ by tools/build_dataset.py; rebuild them.
dataset/

# The training virtualenv: 3 GB of PyTorch, rebuilt by the command in
# tools/train_paint.py. Deliberately separate from .venv39 so the app never
# carries it.
.venv-train/
